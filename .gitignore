node_modules/
dist/
build/
coverage/
*.log
.DS_Store

# Local state — SQLite session store, OAuth caches, etc.
data/
*.sqlite
*.sqlite-*
*.db

# Config with secrets
.env
.env.local
.env.*.local
.claude/
entities/

# Request-private Pi materialization; cleanup is best-effort after hard process exits.
.cli-bridge-pi-profile-*/
.cli-bridge-pi-mcp-*/

# Agent worktrees are separate checkouts of this repo; never commit them as embedded repos.
.worktrees/

---
name: 00_carrier
description: "Skill 00_carrier. Use when the task matches its domain."
---

Before creating any critical incident, consult the `critical_incident_creation_guide.md` document in your workspace. It contains the complete, fixed transaction specification, including all required fields and constant values (like `category=hardware`), and the full ordered procedure. Ensure you set every field explicitly as listed and complete all steps: incident creation, status update to `in_progress`, and sending the queued alert notification, before replying DONE.
